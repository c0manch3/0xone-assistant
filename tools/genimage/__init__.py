"""Thin HTTP client CLI for mflux-server on the Mac host (SSH reverse tunnel).

See `tools/genimage/main.py` for the entrypoint. This package marker is
required so `python -m tools.genimage.main` and relative imports resolve
the same way whether the CLI is invoked via cwd launch or module form.
"""
