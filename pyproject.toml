[project]
name = "0xone-assistant"
version = "0.0.0"
description = "Personal Telegram bot on Claude Agent SDK — rebuild in progress (see plan/)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/assistant"]
